module.exports = {
    name: "logOK",
    execute() {
        //let _userId = xml.body.login.$.id;
        //let _isMod = xml.body.login.$.mod;
        //let _username = xml.body.login.$.n;
        //amIModerator = _isMod == 1;
        //myUserId = _userId;
        //myUserName = _username;
        //playerId = -1;
        //onLogin({ "success": true, "name": _username, "error": o.body.login.$.e });
        //getRoomList();
    }
}